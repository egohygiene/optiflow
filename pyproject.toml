[project]
name = "optiflow-docs"
version = "0.0.0"
description = "Reproducible documentation toolchain for optiflow"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
docs = [
  "zensical==0.0.51",
]

[tool.uv]
package = false
required-version = ">=0.11.33,<0.12"

